[defaults]
#ask_vault_pass = True
vault_password_file = ~/.bin/ansible-vault-pass.sh

gathering = explicit
inventory = staging
#remote_user = notroot

#[privilege_escalation]
#become = True


#[ssh_connection]
#scp_if_ssh=False
