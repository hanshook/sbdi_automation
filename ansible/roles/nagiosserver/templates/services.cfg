define service {
    use                           generic-service
    hostgroup_name                nrpe-clients
    service_description           Package status
    check_command                 check_nrpe_1arg!check_apt
}

define service {
    use                           generic-service
    hostgroup_name                nrpe-clients
    service_description           Disk space
    check_command                 check_nrpe_1arg!check_disk
}

define service {
    use                           generic-service
    hostgroup_name                nrpe-clients
    service_description           CPU load
    check_command                 check_nrpe_1arg!check_load
}

define service {
    use                           generic-service
    hostgroup_name                gluster-servers
    service_description           GLUSTER status
    check_command                 check_nrpe_1arg!check_glusterfs
}

define service {
    use                           generic-service
    hostgroup_name                mdraid-servers
    service_description           MD-RAID status
    check_command                 check_nrpe_1arg!check_md_raid
}

define service {
    use                           generic-service
    hostgroup_name                hardware-servers
    service_description           Temperature sensors
    check_command                 check_nrpe_1arg!check_sensors
}

define service {
    use                           generic-service
    hostgroup_name                unattended-upgrades
    service_description           Reboot required status
    check_command                 check_nrpe_1arg!check_reboot-required
}

define service {
    hostgroup_name                http9000-servers
    service_description           Play framwork servers
    check_command                 check_http!-p 9000
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                http8080-servers
    service_description           Http 8080 servers
    check_command                 check_http!-p 8080
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                external-https-servers
    service_description           External https servers
    check_command                 check_http!-S
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                external-https-servers
    service_description           HTTPS certificate check
    check_command                 check_cert!443!15
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                external-http-servers
    service_description           External http servers
    check_command                 check_http
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                dns-servers
    service_description           DNS servers
    check_command                 check_dns
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                ldap-servers
    service_description           LDAP servers
    check_command                 check_ldap_ssl!dc=altrusoft,dc=se
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                ldap-servers
    service_description           LDAPS certificate check
    check_command                 check_cert!636!15
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                ntp-servers
    service_description           NTP servers
    check_command                 check_ntp
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                smtp-servers
    service_description           SMTP servers
    check_command                 check_smtp
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                smtp-servers
    service_description           SMTPS certificate check
    check_command                 check_cert!465!15
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                imap-servers
    service_description           IMAP servers
    check_command                 check_imap
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}

define service {
    hostgroup_name                imap-servers
    service_description           IMAPS certificate check
    check_command                 check_cert!993!15
    use                           generic-service
    notification_interval         240 ; set > 0 if you want to be renotified
}
